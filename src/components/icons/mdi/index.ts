import {
  mdiClose,
  mdiLoading,
  mdiChevronUp,
  mdiChevronDown,
  mdiFilter,
} from '@mdi/js';

const mdi: { [key: string]: string } = {
  mdiClose,
  mdiLoading,
  mdiChevronUp,
  mdiChevronDown,
  mdiFilter,
};

export default mdi;
